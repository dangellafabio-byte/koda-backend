/**
 * /heart-voice-reveal — Route della fase C del piano V3 (Fabio 2026-08-22).
 * Play della clip "Questo è il mio cuore..." + CTA verso microdemo/paywall.
 * File-based routing Expo, wrapper passthrough al componente.
 */
import React from "react";
import HeartVoiceReveal from "../components/HeartVoiceReveal";

export default function HeartVoiceRevealScreen() {
  return <HeartVoiceReveal />;
}
