fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.5.1
pymongo==4.8.0
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.12
httpx==0.27.2
numpy==2.1.1
litellm==1.51.0
emergentintegrations==0.1.0
elevenlabs==1.9.0
PyNaCl==1.5.0
tavily-python==0.5.0
# deploy-trigger 1780578239
# RIMOSSE (Fabio 2026-06-22 v16, post 7° deploy fail) le seguenti perché NON
# usate da nessun import attivo nel backend e potenzialmente bloccanti su
# immagine ARM Emergent (richiedono libav-dev / libsndfile1 system libs che
# l'immagine cloud_arm potrebbe non avere → pip install in hang fino al
# timeout di 5 min del pipeline):
# - av==17.1.0          (serviva a vad_silero, ora in _archive)
# - onnxruntime==1.27.0 (bloccata da policy Emergent, vedi sopra)
# - soundfile==0.14.0   (serviva a vad_silero, ora in _archive)
imageio-ffmpeg==0.6.0  # static ffmpeg binary for voice_stream.py (AAC→PCM conversion to Deepgram Live)

# === SPEAKER VERIFICATION / VOICEPRINT (2026-07-14, Iter 2) ===
# Riconosce la voce specifica dell'utente Fabio e scarta chunk audio
# provenienti da voci diverse (rumore ambientale, vicini, TV) prima di
# inoltrarli a Deepgram. Vedi backend/voiceprint_service.py.
# NOTE:
# - torch CPU wheel (indice --extra-index-url pytorch cpu) — ~200MB.
#   Su ARM64 Linux pod Emergent funziona (verificato 14/07/2026).
# - resemblyzer usa librosa/scipy/numpy che sono già dipendenze indirette.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
Resemblyzer==0.1.4
librosa==0.11.0
scipy==1.17.1

sentry-sdk==2.66.1
